services:
  - type: web
    name: nutrition-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn nutrition_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false  
